language: java

jdk:
  - oraclejdk8

env:
  - RAILS_ENV=production

script:
  - mvn clean test -Pcoverage-per-test
  - tail -F target/fixtures/sonarqube/sonarqube-6.0/logs/sonar.log &
  - mvn verify

sudo: false

notifications:
  email: false
